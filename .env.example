OPENAI_API_KEY=your_api_key_here
OPENAI_MODEL=gpt-5.6
SF_ORG_ALIAS=hackathon-org
PORT=3001
# Exact Lightning origin(s), comma separated. Never use * in production.
CORS_ORIGINS=http://localhost:3000
# Optional shared secret. Set the same value in the LWC's API token property.
BACKEND_API_TOKEN=
